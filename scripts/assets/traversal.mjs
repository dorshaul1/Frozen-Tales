import fs from 'node:fs';import {generate} from './generate.mjs';import {build,validate} from './core.mjs';
const base=JSON.parse(fs.readFileSync('assets/definitions/shore.json'));const d={...base,id:'thin-channel-ice',renderer:'traversalArt',frameSize:[96,24],transparent:true,type:'prop',description:'Thin translucent-looking ice shelf with jagged snow dusting and branching fractures, native pixel art.',frames:[0,1,2].map(index=>({direction:'N',animation:'idle',index}))};fs.writeFileSync('assets/definitions/thin-channel-ice.json',JSON.stringify(d,null,2)+'\n');generate(d,undefined,false);build();console.log(validate());
