// Shared water exclusion only; instances are owned and cleaned up by NavigationView.
export const driftingIce: {x:number;y:number}[]=[];

export const thinIce:{x:number;y:number;width:number;height:number}[]=[];
